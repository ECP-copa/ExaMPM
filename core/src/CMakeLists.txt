SET(HEADERS "")
SET(SOURCES "")

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

APPEND_SET(HEADERS
  Box.hh
  FileIO.hh
  Geometry.hh
  Mesh.hh
  Particle.hh
  )

APPEND_SET(SOURCES
  FileIO.cc
  Mesh.cc
  Box.cc
  )

TRIBITS_ADD_LIBRARY(
  exampm_core
  HEADERS ${HEADERS}
  SOURCES ${SOURCES}
  )

TRIBITS_ADD_EXECUTABLE(
  exampm
  NOEXESUFFIX
  NOEXEPREFIX
  SOURCES exampm.cc
  INSTALLABLE
  )
